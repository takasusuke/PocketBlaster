# CLAUDE.md（PocketBlaster）

このファイルより先に`../CLAUDE.md`（`AIFiles`共通方針）を必ず読むこと。Git運用・push方針・
安全策・秘密情報の扱い等はすべてそちらが正。ここには本リポジトリ固有の内容のみを書く。

## 概要

スマホを「銃」に見立て、実際に狙いを付けて画面上の敵を撃つ、シングルプレイヤーのオンレール
FPSシューティングゲーム（アーケードのガンシューティング — Time Crisis / House of the Dead /
Point Blank系譜）。PC/TV画面に敵が表示され、プレイヤーはスマホのジャイロセンサーで狙いを
つける。ブラウザだけで動くWebアプリをコントローラーとして使うため、**ネイティブアプリの
配布・ストア審査は不要**。

詳細な経緯・決定事項は[`docs/requirements.md`](docs/requirements.md)、作業再開時は
[`docs/HANDOFF.md`](docs/HANDOFF.md)を先に読む。

**設計判断に迷ったら立ち返る問い**: 「スマホを本物の銃のように構えて、狙って撃つ感触が
そこにあるか？」（キー操作やカーソル選択で妥協した瞬間、このゲームの核が失われる）

## 技術スタック

- ゲーム本体: Unity（3D）
- コントローラー: スマホのブラウザ（`DeviceOrientationEvent`/`DeviceMotionEvent`でジャイロ値を取得）
- 通信: 同一Wi-Fi内のWebSocket（スマホのブラウザ → PC上のゲーム）
- 配布: ネイティブアプリ化・ストア提出なし（PC側はitch.io等での配布を想定、未決 →
  [`docs/requirements.md`](docs/requirements.md)未決事項）

## 設計上の不変条件

以下は覆すと体験の核が壊れる、または後戻りが困難な制約。変更する場合は要件定義に立ち返る。

1. **狙いはスティック/カーソル選択ではなく、スマホの物理的な向きで決める。** ボタン選択に
   逃げない。
2. **スマホは「銃のように構える」持ち方が前提。** カメラがテレビ画面を向かない持ち方なので、
   カメラベースの絶対位置トラッキング（Sinden Lightgun方式等）は採用できない。ジャイロの
   ドリフトは「リロード動作＝画面中央に構え直して再キャリブレーション」で吸収する
   （詳細は`docs/requirements.md`）。
3. **ネイティブアプリ化・ストア審査を前提にしない。** スマホ側はブラウザで完結させる
   （Switch等のハードル回避が動機のため）。

## データ管理

- 大きなアセット（音源・高解像度テクスチャ等）は`AIFiles/assets/PocketBlaster/`にも複製する
  （`../CLAUDE.md`§11「画像は`assets/`に集約して使い回す」と同じ運用）。

## マイルストーンの扱い

`docs/requirements.md`のマイルストーン表を参照。段階的に触れる状態を作り、「狙って撃つ感触」を
早期に検証することを最優先する（ジャンル地図の言う「面白いかの判断は触ってみないと分からない」を
体現する必要がある領域）。
